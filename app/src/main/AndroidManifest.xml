<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.gifu_u.info.onishi.shiftscheduler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShiftScheduler">

        <!-- 最初に起動するActivity（ホーム画面） -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ホームからボタンで遷移できる画面 -->
        <activity android:name=".WorkerListActivity" />
        <activity android:name=".ShiftCreateActivity" />
        <activity android:name=".SavedShiftListActivity" />

    </application>

</manifest>